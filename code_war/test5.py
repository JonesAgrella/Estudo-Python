def remove_char(string):
    list = []
    for char in string:
        list.append(char)
    last_position = len(list) - 1
    list = list[1:last_position]
    
    return 

print(remove_char('jonesagrella'))